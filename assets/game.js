// Assign number values to each crystal picture
var redCrys = 9
var blueCrys = 13
var greenCrys = 3
var whiteCrys = 10
var ComputerNumber = Math.floor(Math.random) * 100

// Your running score

var count = 0
$("#yourScore").text(count)

// Win/loss count

var winCount = 0
$("#winCount").text(winCount)

var lossCount = 0
$("lossCount").text(lossCount)